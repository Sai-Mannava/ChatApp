# Local-first default (no Docker required)
DATABASE_URL=sqlite:///./writer.db
APP_ENV=development

# Optional Postgres example:
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/writer
